package com.ProjetMaBanque.dao;

import org.springframework.data.jpa.repository.JpaRepository;

import com.ProjetMaBanque.entity.Client;

/**
 * Permet de gerer les Entites à l'aide de SpringData
 * Avec SpringData il suffit d'heriter d'une interface generique 
 * JpaRepository<Client,Long> cela nous permet de gerer l'entitee "Client" qui a ID de type "Long ID_code"
 *
 */
public interface ClientRepository  extends JpaRepository<Client,Long>{
//	final Log log = LogFactory.getLog(ClientRepository.class);
//	log.info("");

}
